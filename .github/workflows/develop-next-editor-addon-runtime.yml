name: Migrate editor addons to Rust runtime

on:
  pull_request:
    branches:
      - feature/physical-addon-packages
    paths:
      - .github/workflows/develop-next-editor-addon-runtime.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
        with:
          ref: develop_next
          fetch-depth: 0

      - uses: ./.github/actions/setup

      - name: Publish the engine-neutral editor runtime and migrate Code execution
        shell: bash
        run: |
          set -euxo pipefail
          python3 - <<'PY'
          from pathlib import Path

          root = Path('.')
          resource = root / 'Elephant/frontend/src/renderer/src/muya/editorRuntimeResource.js'
          resource.write_text("""const BLOCK_SELECTOR = '[data-elephant-editor-layer="block"][data-elephant-editor-kind]'

const blockDescriptor = (element) => Object.freeze({
  nodeId: Number(element.getAttribute('data-elephant-editor-node')) || null,
  kind: element.getAttribute('data-elephant-editor-kind') || '',
  language: element.getAttribute('data-language') || '',
  element
})

export const createRustEditorRuntimeBinding = ({ runtime, getMarkdown = () => '' } = {}) => {
  if (!runtime?.bridge || typeof runtime.bridge.dispatch !== 'function') {
    throw new TypeError('A live Rust editor runtime is required')
  }

  const listeners = new Set()
  const root = runtime.domContainer || null
  let disposed = false

  const payload = (detail = {}) => Object.freeze({
    engine: 'rust',
    markdown: String(detail.markdown ?? getMarkdown() ?? ''),
    revision: runtime.bridge.revision,
    selection: runtime.bridge.selection,
    root,
    ...detail
  })

  const notify = (detail = {}) => {
    if (disposed) return
    const event = payload(detail)
    for (const listener of [...listeners]) listener(event)
  }

  const resource = Object.freeze({
    apiVersion: 1,
    owner: 'elephant.core.editor',
    engine: 'rust',
    root,
    getMarkdown: () => String(getMarkdown() ?? ''),
    snapshot: () => runtime.bridge.snapshot(),
    dispatch: (command) => runtime.bridge.dispatch(command),
    queryBlocks(options = {}) {
      const kind = typeof options === 'string' ? options : String(options.kind || '')
      const language = typeof options === 'object' ? String(options.language || '') : ''
      if (!root?.querySelectorAll) return []
      return [...root.querySelectorAll(BLOCK_SELECTOR)]
        .filter((element) => !kind || element.getAttribute('data-elephant-editor-kind') === kind)
        .filter((element) => !language || element.getAttribute('data-language') === language)
        .map(blockDescriptor)
    },
    watch(listener, options = {}) {
      if (typeof listener !== 'function') throw new TypeError('Editor runtime listener must be a function')
      listeners.add(listener)
      if (options.immediate !== false) listener(payload({ reason: 'attached' }))
      return () => listeners.delete(listener)
    }
  })

  return Object.freeze({
    resource,
    notify,
    dispose() {
      disposed = true
      listeners.clear()
    }
  })
}
""")

          elements = root / 'Elephant/frontend/src/muya/lib/rust/domRenderer/elements.js'
          text = elements.read_text()
          anchor = """  element.setAttribute(NODE_ATTRIBUTE, String(node.id))
            element.setAttribute('data-muya-rust-layer', node.kind.layer)
            if (node.kind.value?.type) element.setAttribute('data-muya-rust-kind', node.kind.value.type)
          """
          replacement = """  element.setAttribute(NODE_ATTRIBUTE, String(node.id))
            element.setAttribute('data-muya-rust-layer', node.kind.layer)
            if (node.kind.value?.type) element.setAttribute('data-muya-rust-kind', node.kind.value.type)
            element.setAttribute('data-elephant-editor-node', String(node.id))
            element.setAttribute('data-elephant-editor-layer', node.kind.layer)
            if (node.kind.value?.type) element.setAttribute('data-elephant-editor-kind', node.kind.value.type)
          """
          if anchor not in text:
              raise SystemExit('Rust DOM attribute anchor changed')
          elements.write_text(text.replace(anchor, replacement, 1))

          runtime = root / 'Elephant/frontend/src/renderer/src/components/editorWithTabs/runtimeEditor.vue'
          text = runtime.read_text()
          text = text.replace(
              "import { RustMuyaRuntimeEditor } from '@/muya'\n",
              "import { RustMuyaRuntimeEditor } from '@/muya'\nimport { createRustEditorRuntimeBinding } from '@/muya/editorRuntimeResource'\n",
              1
          )
          text = text.replace(
              "const tableDialogVisible = ref(false)\n\nconst handleRustRuntimeReady = (runtime) => { rustRuntime.value = runtime }\n",
              """const tableDialogVisible = ref(false)
let editorRuntimeBinding = null
let disposeEditorRuntimeResource = null

const unpublishEditorRuntime = () => {
  disposeEditorRuntimeResource?.()
  disposeEditorRuntimeResource = null
  editorRuntimeBinding?.dispose()
  editorRuntimeBinding = null
}

const publishEditorRuntime = () => {
  if (!editorRuntimeBinding || disposeEditorRuntimeResource) return
  const host = globalThis.__ELEPHANT_ADDON_HOST__
  if (typeof host?.provide === 'function') {
    disposeEditorRuntimeResource = host.provide('editor.runtime', editorRuntimeBinding.resource)
  }
}

const handleRustRuntimeReady = (runtime) => {
  unpublishEditorRuntime()
  rustRuntime.value = runtime
  editorRuntimeBinding = createRustEditorRuntimeBinding({
    runtime,
    getMarkdown: () => currentFile.value?.markdown ?? props.markdown
  })
  publishEditorRuntime()
  editorRuntimeBinding.notify({ reason: 'ready' })
}
""",
              1
          )
          text = text.replace(
              """const handleRustMarkdownChange = (editorMarkdown) => {
  applyRustEditorMarkdown({
    editorStore,
    file: currentFile.value,
    editorMarkdown,
    fromEditorMarkdown: props.fromEditorMarkdown,
    persist: debouncedSendBufferedState
  })
}
""",
              """const handleRustMarkdownChange = (editorMarkdown) => {
  applyRustEditorMarkdown({
    editorStore,
    file: currentFile.value,
    editorMarkdown,
    fromEditorMarkdown: props.fromEditorMarkdown,
    persist: debouncedSendBufferedState
  })
  editorRuntimeBinding?.notify({
    reason: 'document-change',
    markdown: props.fromEditorMarkdown(String(editorMarkdown || '')),
    editorMarkdown: String(editorMarkdown || '')
  })
}
""",
              1
          )
          text = text.replace(
              """onMounted(() => {
  for (const [event, handler] of Object.entries(busHandlers)) bus.on(event, handler)
})
""",
              """onMounted(() => {
  for (const [event, handler] of Object.entries(busHandlers)) bus.on(event, handler)
  globalThis.addEventListener?.('elephantnote:addons-ready', publishEditorRuntime)
  publishEditorRuntime()
})
""",
              1
          )
          text = text.replace(
              """onBeforeUnmount(() => {
  for (const [event, handler] of Object.entries(busHandlers)) bus.off(event, handler)
  rustRuntime.value = null
""",
              """onBeforeUnmount(() => {
  for (const [event, handler] of Object.entries(busHandlers)) bus.off(event, handler)
  globalThis.removeEventListener?.('elephantnote:addons-ready', publishEditorRuntime)
  unpublishEditorRuntime()
  rustRuntime.value = null
""",
              1
          )
          runtime.write_text(text)

          trusted = root / 'Elephant/frontend/src/renderer/src/addons/trustedAddonRuntime.js'
          text = trusted.read_text()
          old = """      get active() {
        return host?.get('editor') || host?.get('muya') || target?.marktext?.muya || null
      },
      watch(listener, options) {
        if (!host) throw new Error('Addon host registry is unavailable')
        return track(host.watch('editor', listener, options))
      },
"""
          new = """      get active() {
        return host?.get('editor.runtime') || host?.get('editor') || null
      },
      watch(listener, options) {
        if (!host) throw new Error('Addon host registry is unavailable')
        return track(host.watch('editor.runtime', listener, options))
      },
"""
          if old not in text:
              raise SystemExit('Trusted editor API anchor changed')
          trusted.write_text(text.replace(old, new, 1))

          addon = root / 'addons/official/code-execution/main.js'
          text = addon.read_text()
          text = text.replace(
              """    this.observer = null
    this.config = defaultConfig()
""",
              """    this.observer = null
    this.disposeEditorWatch = null
    this.disposeRuntimeWatch = null
    this.activeRuntime = null
    this.config = defaultConfig()
""",
              1
          )
          old_get_code = """  getCode(block) {
    const code = block.querySelector?.('code')
    return String(code?.textContent ?? block.textContent ?? '')
      .replace(/^(Run|Running…|Copy)\s*/i, '')
      .trimEnd()
  }
"""
          new_get_code = """  getCode(block) {
    const clone = block.cloneNode?.(true)
    clone?.querySelectorAll?.('.elephant-physical-code-toolbar, .elephant-physical-code-output')
      .forEach((element) => element.remove())
    const code = clone?.querySelector?.('code')
    return String(code?.textContent ?? clone?.textContent ?? block.textContent ?? '').trimEnd()
  }
"""
          if old_get_code not in text:
              raise SystemExit('Code extraction anchor changed')
          text = text.replace(old_get_code, new_get_code, 1)

          start = text.index('  scan(root = this.window.document) {')
          end = text.index('\n\n  async renderSettings', start)
          runtime_methods = """  scan(runtime = this.activeRuntime) {
    const blocks = runtime?.queryBlocks?.({ kind: 'code_block' }) || []
    for (const descriptor of blocks) this.decorateBlock(descriptor.element || descriptor)
  }

  attachEditorRuntime(runtime) {
    this.observer?.disconnect()
    this.disposeRuntimeWatch?.()
    this.observer = null
    this.disposeRuntimeWatch = null
    this.activeRuntime = runtime?.engine === 'rust' ? runtime : null
    if (!this.activeRuntime) return

    this.scan(this.activeRuntime)
    this.disposeRuntimeWatch = this.activeRuntime.watch?.(() => this.scan(this.activeRuntime), {
      immediate: false
    }) || null

    const root = this.activeRuntime.root
    if (root && this.window.MutationObserver) {
      this.observer = new this.window.MutationObserver(() => this.scan(this.activeRuntime))
      this.observer.observe(root, { childList: true, subtree: true })
    }
  }

  installEditorRuntime() {
    this.disposeEditorWatch = this.api.editor.watch(({ value }) => this.attachEditorRuntime(value))
  }
"""
          text = text[:start] + runtime_methods + text[end:]
          text = text.replace(
              """  onunload() {
    this.observer?.disconnect()
    this.observer = null
""",
              """  onunload() {
    this.disposeEditorWatch?.()
    this.disposeRuntimeWatch?.()
    this.disposeEditorWatch = null
    this.disposeRuntimeWatch = null
    this.activeRuntime = null
    this.observer?.disconnect()
    this.observer = null
""",
              1
          )
          if '.muya-code-block' in text or 'data-function-type="fencecode"' in text or '.ag-code-block' in text:
              raise SystemExit('Code execution still contains legacy editor selectors')
          addon.write_text(text)

          test = root / 'tests/app/unit/addons/rustEditorAddonRuntime.spec.js'
          test.parent.mkdir(parents=True, exist_ok=True)
          test.write_text("""import fs from 'node:fs'
import path from 'node:path'
import { describe, expect, it, vi } from 'vitest'

import { createRustEditorRuntimeBinding } from '../../../Elephant/frontend/src/renderer/src/muya/editorRuntimeResource'

const root = process.cwd()
const read = (file) => fs.readFileSync(path.join(root, file), 'utf8')

describe('Rust editor addon runtime', () => {
  it('publishes semantic blocks and document change events', async () => {
    const container = document.createElement('section')
    const code = document.createElement('pre')
    code.dataset.elephantEditorNode = '7'
    code.dataset.elephantEditorLayer = 'block'
    code.dataset.elephantEditorKind = 'code_block'
    code.dataset.language = 'python'
    container.append(code)
    const bridge = { revision: 3, selection: null, snapshot: vi.fn(), dispatch: vi.fn() }
    const binding = createRustEditorRuntimeBinding({
      runtime: { bridge, domContainer: container },
      getMarkdown: () => '```python\\nprint(1)\\n```'
    })
    const listener = vi.fn()
    binding.resource.watch(listener)
    expect(binding.resource.engine).toBe('rust')
    expect(binding.resource.queryBlocks({ kind: 'code_block' })[0]).toMatchObject({
      nodeId: 7,
      kind: 'code_block',
      language: 'python'
    })
    binding.notify({ reason: 'document-change' })
    expect(listener).toHaveBeenLastCalledWith(expect.objectContaining({
      engine: 'rust',
      revision: 3,
      reason: 'document-change'
    }))
  })

  it('removes every production dependency on Muya DOM selectors', () => {
    const codeExecution = read('addons/official/code-execution/main.js')
    const trusted = read('Elephant/frontend/src/renderer/src/addons/trustedAddonRuntime.js')
    const renderer = read('Elephant/frontend/src/muya/lib/rust/domRenderer/elements.js')
    const runtime = read('Elephant/frontend/src/renderer/src/components/editorWithTabs/runtimeEditor.vue')
    expect(codeExecution).not.toMatch(/muya-code-block|data-function-type=.fencecode|ag-code-block/)
    expect(codeExecution).toContain("queryBlocks?.({ kind: 'code_block' })")
    expect(trusted).not.toContain("host?.get('muya')")
    expect(trusted).toContain("host?.get('editor.runtime')")
    expect(renderer).toContain("data-elephant-editor-kind")
    expect(runtime).toContain("host.provide('editor.runtime'")
  })
})
""")
          PY

      - name: Validate editor addon bridge
        shell: bash
        run: |
          set -euxo pipefail
          NODE_PATH=Elephant/node_modules Elephant/node_modules/.bin/vitest run \
            --config vite.tauri.config.mjs \
            --root . \
            --environment jsdom \
            tests/app/unit/addons/rustEditorAddonRuntime.spec.js \
            tests/app/unit/addons/editorFeatureAddons.spec.js \
            tests/app/unit/specs/main/elephantnote/editorAndAddonRegression.spec.js
          pnpm tauri:web:build

      - name: Commit the runtime contract and remove this workflow
        shell: bash
        run: |
          set -euxo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/develop-next-editor-addon-runtime.yml
          git add -A
          git commit -m "feat(addons): bind editor extensions to Rust runtime"
          git fetch origin develop_next
          git merge --no-edit origin/develop_next
          git push origin HEAD:develop_next
