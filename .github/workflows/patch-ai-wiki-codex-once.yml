name: Patch AI Wiki Codex once

on:
  push:
    branches:
      - feature/wiki-knowledge-continuation
    paths:
      - .github/workflows/patch-ai-wiki-codex-once.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/wiki-knowledge-continuation
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm
      - uses: dtolnay/rust-toolchain@stable
      - name: Apply focused repair
        shell: bash
        run: |
          python3 <<'PY'
          from pathlib import Path

          def replace_once(path, old, new):
              p = Path(path)
              text = p.read_text()
              if old not in text:
                  raise SystemExit(f"missing marker in {path}: {old[:120]!r}")
              p.write_text(text.replace(old, new, 1))

          # Settings: remove the noisy disconnected sentence and expose model-specific reasoning effort.
          path = 'Elephant/frontend/app/components/settings/AiProviderSettingsPanel.vue'
          replace_once(path, '''            <select
              v-if="form.routes.chat.source === 'codex'"
              v-model="form.routes.chat.model"
            >''', '''            <select
              v-if="form.routes.chat.source === 'codex'"
              v-model="form.routes.chat.model"
              @change="onCodexModelChanged"
            >''')
          replace_once(path, '''          </label>
          <label class="wide"><span>System prompt</span><textarea
            v-model="form.routes.chat.systemPrompt"
            rows="5"
          /></label>''', '''          </label>
          <label v-if="form.routes.chat.source === 'codex'">
            <span>Reasoning</span>
            <select v-model="form.routes.chat.reasoningEffort">
              <option
                v-for="effort in codexReasoningOptions"
                :key="effort.value"
                :value="effort.value"
              >
                {{ effort.label }}
              </option>
            </select>
          </label>
          <label class="wide"><span>System prompt</span><textarea
            v-model="form.routes.chat.systemPrompt"
            rows="5"
          /></label>''')
          replace_once(path, '''      systemPrompt: '',
      temperature: 0.2,''', '''      systemPrompt: '',
      reasoningEffort: 'medium',
      temperature: 0.2,''')
          replace_once(path, '''const routeProviderLabel = (source = '') => ({ 'app-local': 'App Local', codex: 'Codex', disabled: 'Disabled' }[source] || source || 'Disabled')
''', '''const routeProviderLabel = (source = '') => ({ 'app-local': 'App Local', codex: 'Codex', disabled: 'Disabled' }[source] || source || 'Disabled')
const selectedCodexModel = computed(() => codexModels.value.find((model) => (model.model || model.id) === form.value.routes.chat.model) || null)
const normalizeReasoningEntry = (entry) => {
  const value = typeof entry === 'string'
    ? entry
    : entry?.reasoningEffort || entry?.reasoning_effort || entry?.effort || entry?.value || entry?.id || ''
  const normalized = String(value).trim()
  if (!normalized) return null
  const label = typeof entry === 'object' && entry?.label
    ? String(entry.label)
    : normalized === 'xhigh'
      ? 'Extra high'
      : normalized.charAt(0).toUpperCase() + normalized.slice(1)
  return { value: normalized, label }
}
const codexReasoningOptions = computed(() => {
  const model = selectedCodexModel.value || {}
  const advertised = model.supportedReasoningEfforts || model.supported_reasoning_efforts || model.reasoningEfforts || []
  const options = advertised.map(normalizeReasoningEntry).filter(Boolean)
  return options.length ? options : ['low', 'medium', 'high'].map(normalizeReasoningEntry)
})
const applyCodexReasoningDefault = () => {
  const options = codexReasoningOptions.value
  if (options.some((entry) => entry.value === form.value.routes.chat.reasoningEffort)) return
  const model = selectedCodexModel.value || {}
  const preferred = model.defaultReasoningEffort || model.default_reasoning_effort || 'medium'
  form.value.routes.chat.reasoningEffort = options.some((entry) => entry.value === preferred)
    ? preferred
    : options[0]?.value || 'medium'
}
''')
          replace_once(path, '''    codexMessage.value = codexStatus.value.error || (codexStatus.value.connected
      ? ''
      : codexStatus.value.installed
        ? 'Codex is installed but no ChatGPT account is connected.'
        : 'The bundled Codex runtime is missing from this build.')''', '''    codexMessage.value = codexStatus.value.error || (codexStatus.value.installed
      ? ''
      : 'The bundled Codex runtime is missing from this build.')''')
          replace_once(path, '''      if (form.value.routes.chat.source === 'codex' && !form.value.routes.chat.model) {
        form.value.routes.chat.model = codexModels.value.find((model) => model.isDefault)?.model || codexModels.value[0]?.model || codexModels.value[0]?.id || ''
      }''', '''      if (form.value.routes.chat.source === 'codex' && !form.value.routes.chat.model) {
        form.value.routes.chat.model = codexModels.value.find((model) => model.isDefault)?.model || codexModels.value[0]?.model || codexModels.value[0]?.id || ''
      }
      if (form.value.routes.chat.source === 'codex') applyCodexReasoningDefault()''')
          replace_once(path, '''const onChatSourceChanged = () => {
  if (form.value.routes.chat.source === 'codex') {
    form.value.routes.chat.model = codexModels.value.find((model) => model.isDefault)?.model || codexModels.value[0]?.model || codexModels.value[0]?.id || ''
  } else if''', '''const onCodexModelChanged = () => {
  applyCodexReasoningDefault()
  scheduleAutosave('codex-model')
}
const onChatSourceChanged = () => {
  if (form.value.routes.chat.source === 'codex') {
    form.value.routes.chat.model = codexModels.value.find((model) => model.isDefault)?.model || codexModels.value[0]?.model || codexModels.value[0]?.id || ''
    applyCodexReasoningDefault()
  } else if''')

          # Shared persisted AI config loader for commands that are called with message-only payloads.
          path = 'Elephant/backend/tauri/src/tauri_extra_commands.rs'
          replace_once(path, '''#[tauri::command]
pub fn tauri_ai_config_get(app: AppHandle) -> R<Value> {
    let path = provider_config_path(&app)?;
    Ok(read_json(
        path,
        json!({
          "localAi": { "enabled": true, "showModelLibraryInSidebar": true },
          "localRuntime": { "llamaServerMode": "bundled", "llamaServerPath": "", "llamaBaseUrl": "" },
          "providers": { "list": [], "codex": { "connected": false, "mode": "account", "model": "" } },
          "routes": {},
          "localModelSelection": {}
        }),
    ))
}''', '''pub(crate) fn load_ai_config(app: &AppHandle) -> R<Value> {
    let path = provider_config_path(app)?;
    Ok(read_json(
        path,
        json!({
          "localAi": { "enabled": true, "showModelLibraryInSidebar": true },
          "localRuntime": { "llamaServerMode": "bundled", "llamaServerPath": "", "llamaBaseUrl": "" },
          "providers": { "list": [], "codex": { "connected": false, "mode": "account", "model": "" } },
          "routes": {},
          "localModelSelection": {}
        }),
    ))
}

#[tauri::command]
pub fn tauri_ai_config_get(app: AppHandle) -> R<Value> {
    load_ai_config(&app)
}''')

          # Chat: hydrate the saved route, then pass the selected Codex reasoning effort.
          path = 'Elephant/backend/tauri/src/chat_runtime.rs'
          replace_once(path, '''fn knowledge_hits(app: &AppHandle, query: &str, limit: usize) -> Vec<KnowledgeSearchHit> {''', '''fn selected_chat_reasoning_effort(payload: &Value) -> Option<String> {
    let route = ai_config(payload).pointer("/routes/chat").unwrap_or(&Value::Null);
    let effort = text(route, &["reasoningEffort", "reasoning_effort", "effort"]);
    (!effort.is_empty()).then_some(effort)
}

fn with_saved_ai_config(app: &AppHandle, payload: Value) -> Value {
    let mut payload = if payload.is_object() {
        payload
    } else {
        json!({ "message": payload })
    };
    if ai_config(&payload).pointer("/routes/chat").is_none() {
        if let Ok(config) = crate::tauri_extra_commands::load_ai_config(app) {
            if let Some(object) = payload.as_object_mut() {
                object.insert("aiConfig".into(), config);
            }
        }
    }
    payload
}

fn knowledge_hits(app: &AppHandle, query: &str, limit: usize) -> Vec<KnowledgeSearchHit> {''')
          replace_once(path, '''    if payload.get("codexOperation").is_some() {
        return codex_app_server::command(&app, &payload).await;
    }

    let message = last_user_message(&payload);
    let model = selected_chat_model(&payload);
    let source = selected_chat_source(&payload);''', '''    if payload.get("codexOperation").is_some() {
        return codex_app_server::command(&app, &payload).await;
    }

    let payload = with_saved_ai_config(&app, payload);
    let message = last_user_message(&payload);
    let model = selected_chat_model(&payload);
    let source = selected_chat_source(&payload);
    let reasoning_effort = selected_chat_reasoning_effort(&payload);''')
          replace_once(path, '''        let result = codex_app_server::chat(&app, &model, &prompt).await?;''', '''        let result = codex_app_server::chat_with_effort(
            &app,
            &model,
            &prompt,
            reasoning_effort.as_deref(),
        )
        .await?;''')
          replace_once(path, '''            "model": result.model,
            "threadId": result.thread_id''', '''            "model": result.model,
            "reasoningEffort": reasoning_effort,
            "threadId": result.thread_id''')
          replace_once(path, '''    fn extracts_last_user_message() {''', '''    fn reads_reasoning_effort_from_chat_route() {
        let payload = json!({ "aiConfig": { "routes": { "chat": { "reasoningEffort": "high" } } } });
        assert_eq!(selected_chat_reasoning_effort(&payload).as_deref(), Some("high"));
    }
    #[test]
    fn extracts_last_user_message() {''')

          # Codex protocol: carry effort in turn/start while retaining the old chat API.
          path = 'Elephant/backend/tauri/src/chat_runtime/codex_app_server.rs'
          replace_once(path, '''fn turn_start_params(thread_id: &str, model: &str, cwd: &str, prompt: &str) -> Value {
    json!({
      "threadId": thread_id,
      "input": [{ "type": "text", "text": prompt }],
      "model": model,
      "cwd": cwd,
      "approvalPolicy": "never",
      "sandboxPolicy": {
        "type": TURN_READ_ONLY_SANDBOX,
        "networkAccess": false
      }
    })
}''', '''fn normalize_reasoning_effort(value: Option<&str>) -> Option<&str> {
    value
        .map(str::trim)
        .filter(|value| matches!(*value, "minimal" | "low" | "medium" | "high" | "xhigh"))
}

fn turn_start_params(
    thread_id: &str,
    model: &str,
    cwd: &str,
    prompt: &str,
    reasoning_effort: Option<&str>,
) -> Value {
    let mut params = json!({
      "threadId": thread_id,
      "input": [{ "type": "text", "text": prompt }],
      "model": model,
      "cwd": cwd,
      "approvalPolicy": "never",
      "sandboxPolicy": {
        "type": TURN_READ_ONLY_SANDBOX,
        "networkAccess": false
      }
    });
    if let Some(effort) = normalize_reasoning_effort(reasoning_effort) {
        params["effort"] = json!(effort);
    }
    params
}''')
          replace_once(path, '''pub async fn chat(app: &AppHandle, model: &str, prompt: &str) -> R<CodexChatResult> {
    let started = Instant::now();''', '''pub async fn chat(app: &AppHandle, model: &str, prompt: &str) -> R<CodexChatResult> {
    chat_with_effort(app, model, prompt, None).await
}

pub async fn chat_with_effort(
    app: &AppHandle,
    model: &str,
    prompt: &str,
    reasoning_effort: Option<&str>,
) -> R<CodexChatResult> {
    let started = Instant::now();''')
          replace_once(path, '''            "start model={model} prompt_chars={}",
            prompt.chars().count()''', '''            "start model={model} effort={} prompt_chars={}",
            normalize_reasoning_effort(reasoning_effort).unwrap_or("default"),
            prompt.chars().count()''')
          replace_once(path, '''            turn_start_params(&thread_id, model, &cwd_text, prompt),''', '''            turn_start_params(
                &thread_id,
                model,
                &cwd_text,
                prompt,
                reasoning_effort,
            ),''')
          replace_once(path, '''        let params = turn_start_params("thread", "gpt-test", "/tmp/chat", "hello");''', '''        let params = turn_start_params("thread", "gpt-test", "/tmp/chat", "hello", None);''')
          replace_once(path, '''    fn suppresses_high_frequency_event_logs() {''', '''    fn turn_payload_carries_supported_reasoning_effort() {
        let params = turn_start_params("thread", "gpt-test", "/tmp/chat", "hello", Some("high"));
        assert_eq!(params.get("effort").and_then(Value::as_str), Some("high"));
        let invalid = turn_start_params("thread", "gpt-test", "/tmp/chat", "hello", Some("impossible"));
        assert!(invalid.get("effort").is_none());
    }

    #[test]
    fn suppresses_high_frequency_event_logs() {''')

          # Wiki routing: hydrate persisted config, preserve effort and prefer active routes over stale selections.
          path = 'Elephant/backend/tauri/src/knowledge_wikis.rs'
          replace_once(path, '''struct WikiModelRoute {
    provider: String,
    model: String,
}''', '''struct WikiModelRoute {
    provider: String,
    model: String,
    reasoning_effort: Option<String>,
}''')
          replace_once(path, '''    let request = build_wiki_synthesis_request(&topic, title.as_deref(), &sources, max_sections);
    let route = selected_wiki_route(&payload)?;''', '''    let request = build_wiki_synthesis_request(&topic, title.as_deref(), &sources, max_sections);
    let payload = with_saved_ai_config(&app, payload);
    let route = selected_wiki_route(&payload)?;''')
          replace_once(path, '''fn selected_wiki_route(payload: &Value) -> Result<WikiModelRoute, String> {
    const ROUTE_POINTERS: &[&str] = &[
        "/modelSelection/wiki",
        "/modelSelection/wikiWriting",
        "/aiConfig/localModelSelection/wiki",
        "/aiConfig/localModelSelection/wikiWriting",
        "/aiConfig/routes/wiki",
        "/aiConfig/routes/wikiWriting",
        "/config/routes/wiki",
        "/config/routes/wikiWriting",
        "/aiConfig/routes/chat",
        "/config/routes/chat",
        "/modelSelection/chat",
    ];''', '''fn with_saved_ai_config(app: &AppHandle, payload: Value) -> Value {
    let mut payload = if payload.is_object() { payload } else { json!({}) };
    let has_route = payload.pointer("/aiConfig/routes/chat").is_some()
        || payload.pointer("/config/routes/chat").is_some();
    if !has_route {
        if let Ok(config) = crate::tauri_extra_commands::load_ai_config(app) {
            if let Some(object) = payload.as_object_mut() {
                object.insert("aiConfig".into(), config);
            }
        }
    }
    payload
}

fn selected_wiki_route(payload: &Value) -> Result<WikiModelRoute, String> {
    const ROUTE_POINTERS: &[&str] = &[
        "/aiConfig/routes/wiki",
        "/aiConfig/routes/wikiWriting",
        "/config/routes/wiki",
        "/config/routes/wikiWriting",
        "/aiConfig/routes/chat",
        "/config/routes/chat",
        "/modelSelection/wiki",
        "/modelSelection/wikiWriting",
        "/aiConfig/localModelSelection/wiki",
        "/aiConfig/localModelSelection/wikiWriting",
        "/modelSelection/chat",
    ];''')
          replace_once(path, '''            return Ok(WikiModelRoute {
                provider: "local-llama.cpp".into(),
                model: model.to_string(),
            });''', '''            return Ok(WikiModelRoute {
                provider: "local-llama.cpp".into(),
                model: model.to_string(),
                reasoning_effort: None,
            });''')
          replace_once(path, '''            return Ok(WikiModelRoute {
                provider: provider.to_string(),
                model: model.to_string(),
            });''', '''            let reasoning_effort = ["reasoningEffort", "reasoning_effort", "effort"]
                .iter()
                .find_map(|key| object.get(*key).and_then(Value::as_str))
                .map(str::trim)
                .filter(|value| !value.is_empty())
                .map(str::to_string);
            return Ok(WikiModelRoute {
                provider: provider.to_string(),
                model: model.to_string(),
                reasoning_effort,
            });''')
          replace_once(path, '''            return crate::chat_runtime::codex_app_server::chat(app, &route.model, &prompt)
                .await
                .map(|result| result.answer);''', '''            return crate::chat_runtime::codex_app_server::chat_with_effort(
                app,
                &route.model,
                &prompt,
                route.reasoning_effort.as_deref(),
            )
            .await
            .map(|result| result.answer);''')
          replace_once(path, '''        assert_eq!(route.model, "gpt-5.4-mini");
        assert!(is_codex_provider(&route.provider));''', '''        assert_eq!(route.model, "gpt-5.4-mini");
        assert_eq!(route.reasoning_effort, None);
        assert!(is_codex_provider(&route.provider));''')

          # Wiki JSON: exact schema prompt plus tolerant normalization of string claims with inline chunk citations.
          path = 'Elephant/backend/knowledge-core/src/wiki_core.rs'
          replace_once(path, '''use serde::{Deserialize, Serialize};''', '''use serde::{de::Error as _, Deserialize, Deserializer, Serialize};
use serde_json::Value;''')
          replace_once(path, '''pub struct WikiSynthesis {
    pub title: String,
    pub summary: Vec<WikiClaim>,
    #[serde(default)]
    pub sections: Vec<WikiSection>,
    #[serde(default)]
    pub related_wikis: Vec<String>,
}''', '''pub struct WikiSynthesis {
    pub title: String,
    #[serde(default, deserialize_with = "deserialize_claims")]
    pub summary: Vec<WikiClaim>,
    #[serde(default)]
    pub sections: Vec<WikiSection>,
    #[serde(default, alias = "relatedWikis", deserialize_with = "deserialize_strings")]
    pub related_wikis: Vec<String>,
}''')
          replace_once(path, '''pub struct WikiSection {
    pub heading: String,
    #[serde(default)]
    pub claims: Vec<WikiClaim>,
}''', '''pub struct WikiSection {
    pub heading: String,
    #[serde(default, deserialize_with = "deserialize_claims")]
    pub claims: Vec<WikiClaim>,
}''')
          replace_once(path, '''pub struct WikiClaim {
    pub text: String,
    #[serde(default)]
    pub citation_chunk_ids: Vec<String>,
}
''', '''pub struct WikiClaim {
    pub text: String,
    #[serde(default, alias = "citationChunkIds", alias = "citations")]
    pub citation_chunk_ids: Vec<String>,
}

fn strings_from_value(value: &Value) -> Vec<String> {
    match value {
        Value::String(value) => vec![value.trim().to_string()],
        Value::Array(values) => values
            .iter()
            .filter_map(Value::as_str)
            .map(str::trim)
            .filter(|value| !value.is_empty())
            .map(str::to_string)
            .collect(),
        _ => Vec::new(),
    }
}

fn extract_inline_citations(text: &str) -> (String, Vec<String>) {
    let mut citations = Vec::new();
    let mut cursor = text;
    while let Some(start) = cursor.find("[chunk-") {
        let after = &cursor[start + 1..];
        let Some(end) = after.find(']') else { break };
        let candidate = &after[..end];
        if candidate
            .chars()
            .all(|character| character.is_ascii_alphanumeric() || character == '-')
        {
            citations.push(candidate.to_string());
        }
        cursor = &after[end + 1..];
    }
    citations.sort();
    citations.dedup();
    let mut cleaned = text.to_string();
    for citation in &citations {
        cleaned = cleaned.replace(&format!("[{citation}]"), "");
    }
    (cleaned.split_whitespace().collect::<Vec<_>>().join(" "), citations)
}

fn claims_from_value(value: &Value) -> Result<Vec<WikiClaim>, String> {
    match value {
        Value::Null => Ok(Vec::new()),
        Value::String(text) => {
            let (text, citation_chunk_ids) = extract_inline_citations(text);
            Ok(vec![WikiClaim { text, citation_chunk_ids }])
        }
        Value::Array(values) => {
            let mut claims = Vec::new();
            for value in values {
                claims.extend(claims_from_value(value)?);
            }
            Ok(claims)
        }
        Value::Object(object) => {
            let raw_text = ["text", "claim", "content", "summary"]
                .iter()
                .find_map(|key| object.get(*key).and_then(Value::as_str))
                .unwrap_or("");
            let mut citation_chunk_ids = ["citation_chunk_ids", "citationChunkIds", "citations"]
                .iter()
                .find_map(|key| object.get(*key))
                .map(strings_from_value)
                .unwrap_or_default();
            let (text, inline) = extract_inline_citations(raw_text);
            if citation_chunk_ids.is_empty() {
                citation_chunk_ids = inline;
            }
            Ok(vec![WikiClaim { text, citation_chunk_ids }])
        }
        _ => Err("Wiki claims must be a string, object, or array.".into()),
    }
}

fn deserialize_claims<'de, D>(deserializer: D) -> Result<Vec<WikiClaim>, D::Error>
where
    D: Deserializer<'de>,
{
    let value = Value::deserialize(deserializer)?;
    claims_from_value(&value).map_err(D::Error::custom)
}

fn deserialize_strings<'de, D>(deserializer: D) -> Result<Vec<String>, D::Error>
where
    D: Deserializer<'de>,
{
    let value = Value::deserialize(deserializer)?;
    Ok(strings_from_value(&value))
}
''')
          replace_once(path, '''    StructuredModelRequest {
        task: StructuredTask::WriteWikiSection,
        system_prompt: format!(
            "You synthesize a cited local wiki from supplied note chunks. Return only data matching the requested JSON schema. Every factual claim must cite one or more supplied chunk IDs. Never cite a chunk that was not supplied. Do not repeat the same idea across sections. Return no more than {max_sections} sections. related_wikis contains only short concept names suitable for wikilinks, never file paths."
        ),''', '''    let schema = r#"{
  "title": "Short wiki title",
  "summary": [
    { "text": "One factual summary claim.", "citation_chunk_ids": ["chunk-id-from-sources"] }
  ],
  "sections": [
    {
      "heading": "Section heading",
      "claims": [
        { "text": "One factual section claim.", "citation_chunk_ids": ["chunk-id-from-sources"] }
      ]
    }
  ],
  "related_wikis": ["Related concept"]
}"#;
    StructuredModelRequest {
        task: StructuredTask::WriteWikiSection,
        system_prompt: format!(
            "You synthesize a cited local wiki from supplied note chunks. Return exactly one JSON object and no prose or Markdown fences. The object must use this exact shape:\n{schema}\nEvery summary and section claim must be an object with text and citation_chunk_ids; summary is always an array, never a string. Every factual claim must cite one or more supplied chunk IDs. Never cite a chunk that was not supplied. Do not place citation markers inside text. Do not repeat the same idea across sections. Return no more than {max_sections} sections. related_wikis contains only short concept names suitable for wikilinks, never file paths."
        ),''')
          replace_once(path, '''    #[test]
    fn output_is_stable_for_same_sources_and_synthesis() {''', '''    #[test]
    fn accepts_string_summary_with_inline_chunk_citations() {
        let sources = sources();
        let response = json!({
            "title": "Iroh",
            "summary": format!("Iroh provides connectivity. [{}]", sources[0].chunk_id),
            "sections": [],
            "related_wikis": []
        })
        .to_string();
        let rendered = parse_and_render_wiki(&response, "Iroh", &sources, 8).unwrap();
        assert!(rendered.markdown.contains("Iroh provides connectivity."));
        assert_eq!(rendered.citations.len(), 1);
    }

    #[test]
    fn output_is_stable_for_same_sources_and_synthesis() {''')
          PY

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile
      - name: Format Rust
        run: cargo fmt --manifest-path Elephant/backend/knowledge-core/Cargo.toml && cargo fmt --manifest-path Elephant/backend/tauri/Cargo.toml
      - name: Test knowledge core
        run: cargo test --manifest-path Elephant/backend/knowledge-core/Cargo.toml --all-targets -- --nocapture
      - name: Check Tauri
        run: cargo check --manifest-path Elephant/backend/tauri/Cargo.toml --no-default-features
      - name: Run focused frontend lint
        run: pnpm exec eslint Elephant/frontend/app/components/settings/AiProviderSettingsPanel.vue
      - name: Commit repair
        shell: bash
        run: |
          git config user.name "ElephantNote Bot"
          git config user.email "actions@users.noreply.github.com"
          git add Elephant/frontend/app/components/settings/AiProviderSettingsPanel.vue \
            Elephant/backend/tauri/src/tauri_extra_commands.rs \
            Elephant/backend/tauri/src/chat_runtime.rs \
            Elephant/backend/tauri/src/chat_runtime/codex_app_server.rs \
            Elephant/backend/tauri/src/knowledge_wikis.rs \
            Elephant/backend/knowledge-core/src/wiki_core.rs
          git commit -m "Fix Codex reasoning, chat routing and Wiki synthesis"
          git push origin HEAD:feature/wiki-knowledge-continuation
