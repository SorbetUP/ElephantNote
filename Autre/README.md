# Autre

This folder is reserved for code and assets that are no longer part of the
active Elephant app.

Nothing has been moved here yet because the current `pnpm dev` build still uses
the legacy Electron/Vite scaffold under `src/`, plus Muya/editor modules. Moving
those files before replacing the entry points would break the app.
