# Muya Rust engine

This directory mirrors the decomposed JavaScript Muya tree.

Rust modules are added file by file, but runtime activation happens by coherent slices so the editor does not incur a native IPC call for every helper or keystroke. The JavaScript implementation remains the characterization oracle until a complete slice has passed differential validation and has been activated.
