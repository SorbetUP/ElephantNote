# Task Dashboard

Scans Markdown notes across the active vault and generates `Reports/Task Dashboard.md`.

The report contains:

- total open and completed task counts;
- notes containing tasks;
- open tasks grouped by source note;
- completed tasks grouped by source note;
- source line numbers for easier navigation.

The addon reads Markdown files only and never modifies source notes.
